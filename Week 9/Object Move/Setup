myitem [] a;
void setup(){
  size(800,800);
  a=new myitem[5];
  for(int i=0;i<5;i++){
    a[i]=new myitem();
  }
}
void draw(){
  background(255);
  for(int i=0;i<5;i++){
    if(i!=4){
      for(int j=i+1;j<5;j++){
        a[i].collide(a[j]);
      }
    }
    a[i].act();
    a[i].life();
  }
}
